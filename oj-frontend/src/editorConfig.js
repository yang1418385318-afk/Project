export function createEditorOptions() {
  return {
    fontSize: 15,
    minimap: { enabled: false },
    automaticLayout: true,
    cursorBlinking: 'smooth',
    tabSize: 4,
    contextmenu: false,
  }
}
